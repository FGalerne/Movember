{% extends 'base.html.twig' %}

{% block body %}
    <h1>Movember</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movember.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ movember.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ movember.prenom }}</td>
            </tr>
            <tr>
                <th>Moustache</th>
                <td>{{ movember.moustache }}</td>
            </tr>
            <tr>
                <th>Codepostal</th>
                <td>{{ movember.codepostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ movember.ville }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ movember.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('movember_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('movember_edit', { 'id': movember.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
